# 卷积神经网络 CNN

卷积神经网络（Convolutional Neural Network, CNN）是一类专门处理具有网格结构数据（如图像）的深度学习模型。它的核心思想是利用卷积操作提取局部特征。

## 卷积层

卷积层是 CNN 的核心组件。它通过卷积核（filter）在输入特征图上滑动，计算点积得到特征映射。卷积核的权重在训练中学习得到，能自动捕获边缘、纹理等视觉特征。

## 池化层

池化层（Pooling）对特征图下采样，降低维度同时保留关键信息。最大池化取窗口内最大值，平均池化取均值。池化让特征具有平移不变性。

## 激活函数

ReLU 激活函数为网络引入非线性。f(x)=max(0,x)，计算简单且缓解梯度消失问题。相比 Sigmoid，ReLU 在深层网络中训练更稳定。

## 全连接层

全连接层（Fully Connected Layer）通常位于网络末端，将高维特征映射到分类空间。它综合卷积层提取的局部特征，输出最终分类结果。

## 反向传播

训练时通过反向传播算法计算损失函数对各层权重的梯度，配合梯度下降更新参数。卷积层的权重共享特性大幅减少了参数量。
